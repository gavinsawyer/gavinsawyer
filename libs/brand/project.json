{
  "name":        "brand",
  "$schema":     "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot":  "libs/brand/src",
  "targets":     {
    "eslint": {
      "executor": "@nx/eslint:lint",
      "options":  { "lintFilePatterns": [ "libs/brand/src/**/*.ts" ] }
    },
    "esbuild": {
      "dependsOn": [ "eslint" ],
      "executor":  "@nx/esbuild:esbuild",
      "options":   {
        "main":       "libs/brand/src/lib/main.ts",
        "outputPath": "dist/libs/brand",
        "tsConfig":   "libs/brand/tsconfig.lib.json"
      },
      "outputs":   [ "{options.outputPath}" ]
    },
    "build":   {
      "dependsOn": [ "esbuild" ],
      "executor":  "nx:noop"
    }
  }
}
