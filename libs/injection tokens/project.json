{
  "name":        "injection-tokens",
  "$schema":     "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot":  "libs/injection tokens/src",
  "targets":     {
    "eslint": {
      "executor": "@nx/eslint:lint",
      "options":  { "lintFilePatterns": [ "libs/injection tokens/src/**/*.ts" ] }
    },
    "esbuild": {
      "dependsOn": [ "eslint" ],
      "executor":  "@nx/esbuild:esbuild",
      "options":   {
        "main":       "libs/injection tokens/src/lib/main.ts",
        "outputPath": "dist/libs/injection tokens",
        "tsConfig":   "libs/injection tokens/tsconfig.lib.json"
      },
      "outputs":   [ "{options.outputPath}" ]
    },
    "build":   {
      "dependsOn": [ "esbuild" ],
      "executor":  "nx:noop"
    }
  }
}
